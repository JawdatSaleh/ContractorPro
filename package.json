{
  "name": "contractorpro",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "echo 'No lint configured'",
    "test": "pnpm -r test"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
